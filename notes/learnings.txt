parser
- for {} is a while true loop (recap)
- bufio.Reader for reading from stdin

tcp server
- goroutine for each accepted connection, pass conn to handleConnection
- net.Listen, pass "tcp" as a parameter
- tried tcp.ListenTcp, but that needs a tcp struct with tcp, ip, zone info
- netcat for easy connections from a different terminal 

reading tcp bytes
- tcp is a stream of bytes
- allocate buffer byte array (1024 is usually enough)
- tried to convert to string and parse for cli args, failed
- failed because this also converted null bytes as part of the string, "\x00"
- had to use bytes.Trim to fix this, then convert using casting with string 

routing / command dispatcher
- tagged switch is better than if block
- strconv.Atoi for string to int
- val, ok for checking existence in a go map

file io
- use path for string, create new constructor for logger
- os.Open for files 
- os.OpenFile for passing in perms and modes like append
- file.WriteString instead of only using a buffer
- scanner.Scan bufio reads line by line efficiently
- append function
- slice headers and copying by value